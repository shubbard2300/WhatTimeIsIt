# ==============================================================================
# ENVIRONMENT VARIABLES FOR "WHAT TIME IS IT?" APP
# ==============================================================================
# Copy this file to .env.local for local development
# In production (Vercel), set these in your project settings
# ==============================================================================

# ------------------------------------------------------------------------------
# DATABASE (Vercel Postgres)
# ------------------------------------------------------------------------------
# When using Vercel Postgres, these are automatically provided in production.
# For local development, create a Vercel Postgres database and copy the connection string.
# Get this from: Vercel Dashboard > Storage > Your Database > .env.local tab

POSTGRES_URL="your-postgres-connection-string-here"
POSTGRES_PRISMA_URL="your-prisma-connection-string-here"
POSTGRES_URL_NO_SSL="your-no-ssl-connection-string-here"
POSTGRES_URL_NON_POOLING="your-non-pooling-string-here"
POSTGRES_USER="default"
POSTGRES_HOST="your-host.postgres.vercel-storage.com"
POSTGRES_PASSWORD="your-password-here"
POSTGRES_DATABASE="verceldb"

# ------------------------------------------------------------------------------
# TWILIO (Required for making phone calls)
# ------------------------------------------------------------------------------
# Get these from: https://console.twilio.com/
# 1. Account SID and Auth Token from the Console Dashboard
# 2. Buy a phone number with Voice capabilities for TWILIO_FROM_NUMBER

TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="your_auth_token_here"
TWILIO_FROM_NUMBER="+15551234567"

# ------------------------------------------------------------------------------
# PUBLIC BASE URL (Required for Twilio callbacks)
# ------------------------------------------------------------------------------
# This is your deployed app's URL. Twilio needs this to fetch TwiML instructions.
# - Production: https://your-app.vercel.app
# - Local dev: Use ngrok or similar tunnel for testing Twilio locally
#   (e.g., ngrok http 5173, then use the https URL ngrok provides)

PUBLIC_BASE_URL="https://your-app.vercel.app"

# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
#
# 1. VERCEL POSTGRES:
#    - Go to Vercel Dashboard > Storage > Create Database > Postgres
#    - Copy the environment variables to your .env.local
#    - Run migrations: npm run db:push
#
# 2. TWILIO SETUP:
#    - Sign up at https://www.twilio.com/try-twilio
#    - Get your Account SID and Auth Token from the Console
#    - Buy a phone number with Voice capabilities
#    - Copy credentials to .env.local
#
# 3. LOCAL TESTING:
#    - For Twilio callbacks to work locally, use ngrok:
#      ngrok http 5173
#    - Copy the https URL to PUBLIC_BASE_URL
#
# ==============================================================================
